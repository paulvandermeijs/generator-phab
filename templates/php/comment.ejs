<% if (locals.lines && locals.lines.length > 0) { %>
    <%= (locals.begin || '/**') %>
    <% (locals.lines || []).forEach(function (line) { -%>
        <%= (locals.prefix || ' * ') %><%- line %>
    <% }) -%>
    <%= (locals.end || ' */') -%>
<% } %>